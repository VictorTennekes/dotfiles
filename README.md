# dotfiles

In this repository I keep my dotfiles used for setting up my VIM and prompt.

## Usage
Execute the `setup.sh` with `spaceship` or `geometry` as the argument to configure either prompt.

You will be prompted to install the fonts after.